domain_url: "https://blog.finolifilms.com"

paginate: 3

paginate_path: "/:num/"

gems:
  - jekyll-paginate

defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
